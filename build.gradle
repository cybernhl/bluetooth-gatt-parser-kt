plugins {
    id "org.jetbrains.kotlin.jvm" version '1.9.20'
}

group = "funny.buildapp.bluetooth"
version = "1.0-SNAPSHOT"

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

